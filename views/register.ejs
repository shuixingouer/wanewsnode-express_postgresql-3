<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1,user-scalable=0">
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/weui.css'/>
    <link rel='stylesheet' href='/stylesheets/example.css'/>
  </head>
  <body>
    <div class="page input js_show">
      <div class="page__hd">
        <h1 class="page__title">欢迎来到Wanews</h1>
        <p class="page__desc">祝你玩得开心</p>
      </div>
      <div class="weui-cells__title">注册</div>
      <div class="weui-cells weui-cells_form">
        <div class="weui-cell">
            <div class="weui-cell__hd">
                <label class="weui-label">昵称</label>
            </div>
            <div class="weui-cell__bd">
                <input class="weui-input" id="username" type="number" pattern="[0-9]*" placeholder="请输入昵称">
            </div>
        </div>
        <div class="weui-cell">
          <div class="weui-cell__hd">
              <label class="weui-label">手机号</label>
          </div>
          <div class="weui-cell__bd">
              <input class="weui-input" id="telnum" type="tel" placeholder="请输入手机号">
          </div>
        </div>
        <div class="weui-cell">
            <div class="weui-cell__hd">
                <label class="weui-label">密码</label>
            </div>
            <div class="weui-cell__bd">
                <input class="weui-input" id="password" type="number" pattern="[0-9]*" placeholder="请输入密码">
            </div>
        </div>
        <div class="weui-cell">
            <div class="weui-cell__hd">
                <label class="weui-label">确认密码</label>
            </div>
            <div class="weui-cell__bd">
                <input class="weui-input"  id="password1" type="number" pattern="[0-9]*" placeholder="请再输入密码">
            </div>
        </div>
      </div>
      <div class="weui-btn-area">
        <a href="javascript:;" class="weui-btn weui-btn_primary">注册</a>
      </div>
      <div class="weui-cells__tips">已经注册？<a href="/login">点击登录</a></div>
    </div>
    <script src="/javascripts/zepto.min.js"></script>
    <script>
			$("#username").blur(function(){
                var username=$("#username").val();
                var telnum=$("#telnum").val();
                var password=$("#password").val();
                var password=$("#password1").val();
                var data={"uname":username};
                if(username.length!=0){
                    $.ajax({
                        url:'/register',
                        type:'post',
                        data:data,
                        success:function(data,status){
                            if(status == 'success'){
                                var obj = JSON.parse(data);
                                alert(obj.type);
                            }
                        },
                        error:function(data,status){
                            if(status == 'error'){
                                var obj = JSON.parse(data);
                                alert(obj.type);
                            }
                        }
                    })
                }
            })
    </script>
  </body>
</html>
